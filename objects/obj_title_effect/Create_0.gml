text = "I'm jackin off!"

alp = 0

// Initialize variables for the oscillation effect
scale_speed = (0.008); // Base speed for scale adjustment
scale_direction = 1; // 1 means growing, -1 means shrinking

// Configurable max and min scale
max_scale = 2.2;
min_scale = 2.0;
scale = min_scale;

sec_text_offset = 3.5

// Ease factor (controls how much easing affects speed)
ease_factor = (0.05);

// Rotation setup
rotation = 15; // Set the desired rotation angle (degrees)
